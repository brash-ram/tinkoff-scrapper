app:
    test: value
    token: ${BOT_TOKEN}

rabbit:
    queue: scrapper-queue
    exchange: scrapper-exchange

baseUrl:
    scrapper: ${SCRAPPER_URL}

default:
    timeout: 5

server:
    port: 8080

spring:
    rabbitmq:
        host: ${RABBIT_HOST}
        username: user
        password: qwerty
        port: 5672
        listener:
            simple:
                default-requeue-rejected: false